{
  "routes": [
    {
      "method": "GET",
      "path": "/resume-skills",
      "handler": "resume-skill.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/resume-skills/count",
      "handler": "resume-skill.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/resume-skills/:id",
      "handler": "resume-skill.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/resume-skills",
      "handler": "resume-skill.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/resume-skills/:id",
      "handler": "resume-skill.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/resume-skills/:id",
      "handler": "resume-skill.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
